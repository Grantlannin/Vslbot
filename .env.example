# Anthropic (server only — used by /api/claude)
ANTHROPIC_API_KEY=

# Supabase (browser client)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional: HTTP Basic gate for /pipeline (omit locally to disable)
PIPELINE_PASSWORD=
# Optional username for Basic auth (default: admin)
# PIPELINE_BASIC_USER=admin
